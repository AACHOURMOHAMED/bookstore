import React from 'react';
import Input from '../components/UI/Input';

const Categories = () => (
  <div>
    <Input
      input={{
        type: 'button',
        name: '',
        value: 'Check status',
        onChange: () => {},
      }}
    />
  </div>
);

export default Categories;
